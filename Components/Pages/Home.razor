@page "/Home"
@attribute [Authorize]
<PageTitle>Home</PageTitle>
<AuthorizeView>
	<Authorized>
	<h1>Hello, @context.User.Claims.FirstOrDefault(c => c.Type == ClaimTypes.Name)?.Value
	</h1>

	Welcome to SparksWare...
	</Authorized>
</AuthorizeView>

